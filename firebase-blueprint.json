{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user details and roles.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["Admin", "Mentor/Teacher", "Protection Officer", "Disaster Management Officer", "User"] },
        "schoolId": { "type": "string", "description": "Optional school ID for school-based users" }
      },
      "required": ["uid", "email", "role"]
    },
    "Report": {
      "title": "Incident Report",
      "description": "A report submitted by a user.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "category": { "type": "string", "enum": ["FGM Risk", "Flood Alert", "Emergency", "Other"] },
        "location": { "type": "string" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "In Progress", "Resolved"] },
        "authorUid": { "type": "string", "description": "UID of the reporter (optional for anonymous)" },
        "timestamp": { "type": "string", "format": "date-time" },
        "resolvedAt": { "type": "string", "format": "date-time", "description": "Timestamp when the report was resolved" },
        "isAnonymous": { "type": "boolean" }
      },
      "required": ["category", "location", "description", "status", "timestamp"]
    },
    "Alert": {
      "title": "System Alert",
      "description": "Flood or emergency alerts broadcast to all users.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "type": { "type": "string" },
        "location": { "type": "string" },
        "message": { "type": "string" },
        "severity": { "type": "string", "enum": ["Low", "Medium", "High", "Critical"] },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["type", "location", "message", "severity", "timestamp"]
    },
    "Donation": {
      "title": "Sponsor Donation",
      "description": "A financial donation record for safe haven sponsorship, stored securely without raw payment credentials.",
      "type": "object",
      "properties": {
        "amount": { "type": "number", "description": "Donation sum in KES" },
        "email": { "type": "string", "format": "email" },
        "cardholderName": { "type": "string" },
        "cardBrand": { "type": "string" },
        "cardLast4": { "type": "string" },
        "status": { "type": "string", "enum": ["Successful", "Failed"] },
        "transactionType": { "type": "string" },
        "chargeId": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["amount", "status", "cardLast4", "chargeId"]
    }
  },
  "firestore": {
    "/users/{uid}": {
      "schema": "UserProfile",
      "description": "User profile documents"
    },
    "/reports/{reportId}": {
      "schema": "Report",
      "description": "Incident reports"
    },
    "/alerts/{alertId}": {
      "schema": "Alert",
      "description": "System-wide alerts"
    },
    "/donations/{donationId}": {
      "schema": "Donation",
      "description": "Sponsor donation transactions securely logged without raw credential storage"
    }
  }
}
